//! The project's grouped-block CSV format: block framer, parser, import
//! profiles and the round-tripping writer.
//!
//! Filled in at milestone M2 — Import (see `docs/DESIGN.md` §16). The crate exists
//! now so the workspace dependency graph and CI are in place from M0.
