//! Parametric signal generation: the `GenSpec` node tree and its renderer.
//!
//! Filled in at milestone M3 — Generate (see `docs/DESIGN.md` §16). The crate exists
//! now so the workspace dependency graph and CI are in place from M0.
