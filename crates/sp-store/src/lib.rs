//! Persistence for SignalPlayback: SQLite metadata, migrations, and the
//! content-addressed sample blob store.
//!
//! Filled in at milestone M1 — Store (see `docs/DESIGN.md` §16). The crate exists
//! now so the workspace dependency graph and CI are in place from M0.
